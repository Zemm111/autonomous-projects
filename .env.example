# OpenClaw Configuration
# Contact form uses OpenClaw CLI for Telegram notifications
# Ensure openclaw is installed and configured: https://openclaw.com

# No environment variables required - uses system OpenClaw configuration
